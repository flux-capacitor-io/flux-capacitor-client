language: java

jdk:
  - oraclejdk11

cache:
  directories:
    - ~/.m2/repository

install: mvn compile

deploy:
  provider: script
  script: ".travis/deploy.sh"
  skip_cleanup: true
  on:
    branch: master
    tags: true
    condition: "$TRAVIS_TAG =~ ^[0-9].*$"
