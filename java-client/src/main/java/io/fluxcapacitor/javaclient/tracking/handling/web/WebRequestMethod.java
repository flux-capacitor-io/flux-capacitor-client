package io.fluxcapacitor.javaclient.tracking.handling.web;

public enum WebRequestMethod {
    GET, POST, PUT, PATCH, DELETE, HEAD, OPTIONS, TRACE,
    WS_OPEN, WS_MESSAGE, WS_CLOSE, WS_PING, WS_PONG, WS_ERROR, ANY;
}
